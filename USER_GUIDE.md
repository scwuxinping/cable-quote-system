# 电缆报价系统 · 使用手册

写给厂里使用本系统的同事（业务员、经理、老板）。不懂技术也能照着做。

---

## 一、第一次开机（装好后做一次）

电脑上需先装好 Python（官网 python.org 下载 3.13 版，安装时勾选 **Add to PATH**）。

1. **初始化**：双击项目文件夹里的 `init.bat`，等它跑完（第一次要联网下载依赖，几分钟）。
2. **启动**：双击 `start.bat`，看到"启动中"就成功了。这个黑窗口**用的时候别关**，下班关掉即停系统。
3. **访问**：本机浏览器打开 `http://127.0.0.1:8000`；
   同事电脑打开 `http://这台电脑的IP:8000`（IP 用 `ipconfig` 命令查"IPv4 地址"，
   第一次要在这台电脑上放行防火墙 8000 端口）。
4. **开机自动干活**（建议）：双击 `install_tasks.bat`，注册两个每日任务：
   - 每早 7:00 自动备份数据库（`backups/` 文件夹，保留最近 30 份）
   - 每早 7:30 自动抓取当日铜价

> 想让全厂随时能访问、重启后自动运行？让懂电脑的人看 `start_prod.bat`（生产模式）。

## 二、登录与账号

系统初始化后自带 4 个账号，**正式使用前必须逐个改密码**：

| 账号 | 初始密码 | 角色 | 用途 |
|------|---------|------|------|
| admin | admin1234 | 超级管理员 | 后台维护（一般不用直接登录系统） |
| manager | manager123 | 经理 | 审批报价、看全部数据、审计日志、账号管理 |
| boss | boss123 | 老板 | 大单终审、看全部数据 |
| sales | sales123 | 业务员 | 日常建报价单，只能看自己的单据 |

**改密码**：登录后点右上角自己的用户名（改密码），填原密码 + 新密码（至少 6 位）即可。
所有改密、建号操作都会记入审计日志。

**给新同事开账号**：经理登录 → 顶部导航"账号" → 填用户名、选角色、给个初始密码 → 新建账号。
把账号密码**当面**告诉同事，提醒他首次登录后自己改密码。同事离职时点"停用"，他的单据数据仍保留。

**忘了密码**：找经理在"账号"页重置；经理忘了找老板；老板忘了用 admin 进后台改。

## 三、每天怎么用

**业务员**：
1. 客户来询价 → 工作台"快速询价"输 `YJV 3x120+1x70` 秒出价格，顺带看最近成交参考；
2. 谈得差不多 → "新建报价单"，加明细、调折扣运费，提交；毛利不够会自动进经理审批；
3. 审批通过 → 报价单详情页发邮件给客户，或生成链接发微信让客户在线看、在线签收；
4. 客户确认 → 一键"转订单"；有 Excel 询价清单的，用"Excel 导入"一次生成草稿。

**经理**：
- 工作台看"待审批"，点进去看毛利（成本只有经理可见），通过/驳回；
- 转订单时系统自动查客户信用额度，超了会拦截，确实要发货可"强制转订单"（会留痕）；
- "日志"页可查所有关键操作（谁改了价、谁转了单、谁收了款）。

**老板**：
- 大单（默认 50 万以上）经理通过后还需你终审；
- 工作台和"报表"看每月报价、订单、回款汇总；
- 注意工作台顶部的红色预警：**到期未签收**的报价单要及时跟单。

**客户自己询价**：把 `http://你们的IP:8000/portal/` 发给客户，客户免登录查目录价、提交询价，
线索自动出现在"询价线索"，业务员一键转报价单（客户看不到成本）。

## 四、价格与参数

- **铜价**：每早自动抓（上期所沪铜）；也可在"价格维护"手工改，或命令行
  `py -3.13 manage.py fetch_cu_price --lme 10500 --rate 7.15`（LME 换算）。
- **参数**（最低毛利、大单阈值、各项费率）：admin 登录 `/admin/` → Pricing params 调整。
- **客户阶梯价 / 等级折扣**：客户管理页里维护，报价自动套用。

## 五、备份与恢复

- **备份**：每天 7:00 自动备份到 `backups\`（也可手动双击 `backup.bat`）。
- **恢复**：关掉 start.bat 窗口 → 把 `backups\db_日期.sqlite3` 复制一份改名为 `db.sqlite3`
  放回项目根目录覆盖 → 重新启动。
- 迁移到新电脑：整个文件夹拷过去，新电脑装好 Python 后双击 `init.bat`（不会清数据，只补依赖），
  再双击 `start.bat`。

## 六、常见问题

| 问题 | 处理 |
|------|------|
| 打不开网页 | start.bat 黑窗口是否开着？地址对不对？同网段？防火墙放行 8000 |
| 登录不上 | 账号是否被停用？密码忘了找经理重置 |
| 询价提示"缺少价格" | 当日铜价未录入，去"价格维护"补，或等每早自动抓取 |
| 铜价没自动更新 | 当天 7:30 电脑是否开机；`cu_price.log` 看报错；可手工补 |
| 误删数据 | 从 `backups\` 按上面的方法恢复最近一份 |
| 规格库里没有的型号 | 报价页可自助添加（填芯数×截面，重量按国标模型自动算）；特殊结构找 admin 后台加 |

## 七、安全提醒（重要）

1. **初始化后立刻改掉 4 个初始密码**（admin 的在 `/admin/` 后台改，其余在系统里改）；
2. 系统只应在**厂内局域网**使用，不要直接暴露到公网；需要外网访问请找专业人士加 VPN；
3. 定期确认 `backups\` 里有新备份；有条件的把备份目录同步到另一块硬盘；
4. 客户门户链接免登录，端口和 IP 不要发给无关人员。
